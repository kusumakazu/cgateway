apply plugin: "war"

bootWar {
    mainClassName = "com.id.kusumakazu.CgatewaysvcApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
